import ProfileBar from "../../components/ProfileBar";

/*
* The profile page just renders the profile bar so it is only rendered once for all the pages
*
* @author Grace Llewellyn
*/

function Profile() {
    return (
        <ProfileBar />
    );
}

export default Profile;